export function renderEvents(config) {
    const events = config.events || [{ title: 'Nikah Ceremony', date: config.date, time: config.time, venue: config.venue, address: config.address, mapsUrl: config.googleMapsUrl }];
    return `<section class="events-section" data-reveal><div class="section-wrap"><p class="eyebrow">Save the date</p><h2>Celebration details</h2><div class="gold-rule"></div><div class="event-grid">${events.map((event, index) => `<article><span class="event-number">${String(index + 1).padStart(2, '0')}</span><h3>${event.title || event.type || 'Event'}</h3><p data-date>${event.date}</p><p>${event.time}</p><p class="muted">${event.venue}</p><p class="muted">${event.address}</p>${event.mapsUrl ? `<a class="text-link" href="${event.mapsUrl}" target="_blank" rel="noreferrer">Open map ↗</a>` : ''}</article>`).join('')}</div></div></section>`;
}
